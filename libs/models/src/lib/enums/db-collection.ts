export enum DbCollection {
  users = 'users',
  resetPassword = 'reset-password',
  attachments = 'attachments',
  state = 'states',
  city = 'cities',
  pass = 'passes',
  reason = 'reasons',
  otpRequest = 'otp-requests'
}
