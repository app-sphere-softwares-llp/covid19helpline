export enum UserStatus {
  'deleted' = 'deleted',
  'blocked' = 'blocked',
  'Active' = 'Active',
  'Left' = 'Left',
  'Expired' = 'Expired'
}

export enum UserLoginProviderEnum {
  'google' = 'google',
  'normal' = 'normal'
}
